[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "track_qms_kanban"
authors = [{name = "CFG", email = "engineering@example.com"}]
description = "Optional TrackQMS and CFG Kanban integration for Frappe"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[tool.ruff]
line-length = 110
target-version = "py310"
